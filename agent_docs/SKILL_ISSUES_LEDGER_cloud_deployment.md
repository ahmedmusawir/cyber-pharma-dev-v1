# SKILL ISSUES LEDGER — Cloud Deployment (family v3.0)

> Mandatory artifact per family CLAUDE.md §4.9. One row per issue, recorded live.
> App: **cyber-pharma-v1** · Activated: 2026-07-23 · Skill: `_SKILLS/stark-cloud-deploy-skills/`

| Date | Phase | Issue | Root Cause | Fix Applied | Skill Change Needed |
|------|-------|-------|-----------|-------------|---------------------|
| 2026-07-23 | Activation + Generate P1 | Agent printed Plan-Mode summary and intake table to screen WITHOUT writing response files first (project CLAUDE.md RESPONSE LOGGING PROTOCOL); operator had to cue "log it" | Agent treated skill-driven artifacts as exempt from the project-level logging protocol; they are not — both protocols fire | Both artifacts retro-logged (`response_2026-07-23_170918_{deploy-activation-plan, deploy-intake-summary}.md`); logging resumed as write-before-screen | Consider a family CLAUDE.md note: "host-project logging protocols still apply during skill runs" |
| 2026-07-23 | Activation | Activation plan printed to screen ~15:10; artifact logged 17:09 only on operator cue | Write-first rule stated in prose but NOT a numbered blocking step in the activation sequence | Retro-logged both artifacts; write-before-screen resumed | Make "write plan artifact to RESPONSES/" an explicit numbered step BEFORE "present plan on screen" in the activation sequence |
| 2026-07-23 | Generate P1 | Naming convention drift — MC service is `cyph-mission-ctrl-prod` ("-prod" suffix in a staging project; slug style `cyph-` vs `cyber-`) | Playbook has one universal `{app-name}-prod` convention with no environment-suffix scheme | `cyber-pharma-prod` kept for consistency this run (operator-ruled) | Playbook needs an environment-suffix naming section (stg/prod) for when real data environments split |
| 2026-07-23 | Path B Step 0 | §4.5 "simple build-SA reveal" returned EMPTY serviceAccount; empty `$BUILD_SA` then broke the IAM filter with a cryptic "Term operand expected" error | (a) region-less `builds list` can miss regional builds; (b) `serviceAccount` field empty on default-SA builds; (c) compound command has no empty-var guard | Retry issued: resolve PROJECT_NUMBER → check compute default SA roles directly (the §5.2 default-SA fact) | §4.5: add `--region` to the simple reveal, an empty-result guard, and a "default-SA builds report empty serviceAccount — fall back to PROJECT_NUMBER-compute@" note |
| 2026-07-23 | Path B Step 0 | Terminal paste split the backslash-continuation command — `--filter` ran as its own command ("command not found"); the UNFILTERED role table nearly passed as filtered EVIDENCE (run.admin visible but member unproven) | Trailing-backslash multi-line commands are fragile under operator copy-paste; same class as v2 Issue #5 (`--substitutions` continuation bug) | Re-issued as a SINGLE-LINE command with project number baked in; evidence discipline held (gate kept open) | Guidance-mode rule: ALL operator paste-back commands single-line or heredoc — never trailing-backslash continuations |
| 2026-07-23 | Generate P2 (post-delivery, operator file review) | Generated package had NO `.dockerignore` while Dockerfile does `COPY . .` — secret-leak path: Next standalone copies `.env` files into `.next/standalone/`, so `.env.local` ships in the final image, bypassing the 3-Layer secret model | TEMPLATES.md Template 1 ships `COPY . .` with no companion `.dockerignore` template; generate SKILL.md has no gate for it | `.dockerignore` created (operator-specified list, `.env*` first); ignore-chain verified (`.gitignore:28` `.env*.local` covers Cloud Build upload; no bare `.env` exists); local checklist gains verification item | TEMPLATES.md: Dockerfile/.dockerignore must ship as a MANDATORY PAIR (new Template 1b); checklist template gains a `.dockerignore` verification line item |
| 2026-07-23 | Generate P2 (operator file review of init-app.sh) | Propagation guard warn-and-continue on timeout: 6th failed attempt printed a warning then CONTINUED into add-iam-policy-binding against a nonexistent SA | Template 4's retry loop narrates the failure instead of blocking on it | Timeout branch now prints warning and `exit 1` immediately | Template 4: guard must exit 1 on timeout — a guard that narrates instead of blocking = deferred confusing failure |
| 2026-07-23 | Generate P2 (operator file review of init-app.sh) | Verification only checked secret EXISTENCE — a green init with the placeholder value still in the secret passes silently | Template 4 verification section has no placeholder detection | Version-count klaxon added (versions list, no values read): 1 version = loud placeholder warning + exact versions-add command; 2+ = ✅ | Template 4: verification must detect placeholder-only secrets via version count — placeholder shipping = runtime failure at first server-side Supabase call, worst possible discovery timing |
| 2026-07-24 | Execute P0 (operator correction) | Generated deploy.sh shipped with REPLACE_ME placeholders for public var values, expecting operator file edits — operator does NOT do manual file edits, ever (v3 regression; MC run baked values) | Generate intake collected public var NAMES but not VALUES | Both Supabase public values baked into deploy.sh by agent on operator instruction | Intake must collect all public var VALUES verbatim at the start and bake them at generation — REPLACE_ME in a generated file = generation defect for public vars |
| 2026-07-24 | Execute P0 (operator ruling) | Secret-value flow was framed as generic "operator updates values afterward" | No canonical definition of the operator's single manual moment | Flow pinned: exactly ONE operator CLI action, between init-app.sh and deploy.sh — agent hands over versions-add with a clearly marked placeholder, operator substitutes his secret in his own terminal, agent never sees the value | v3.1 must state this as the ONLY manual step in the entire pipeline |
| 2026-07-24 | Execute P0 (operator ruling) | Division-of-labor principle implicit, not written | Doctrine never states it | Recorded: operator touches the CLI, never the files — files are born complete | Add as family doctrine principle (CLAUDE.md §4) |
| 2026-07-24 | Execute P0 (operator ruling) | Execute skill opens with routing questions but no operator-facing roadmap | Phase list lives in agent-facing SKILL.md only | Roadmap presented on screen this run (see artifact) | Execute skill must OPEN with an operator roadmap: numbered list of all phases ahead + exactly where the operator's manual CLI moments are |
| 2026-07-24 | Execute P1 (first init-app.sh run) | Propagation guard PASSED (`describe` OK) yet `secrets add-iam-policy-binding` still 400'd "Service account does not exist" — script exited at binding step (set -e) | describe-visibility ≠ binding-visibility: IAM `get` propagates before Secret Manager's IAM backend sees the SA; guard polls the wrong signal | Re-run of idempotent init-app.sh (time elapsed → binding succeeds on pass 2) | Template 4: retry loop must wrap the BINDING call itself (retry add-iam-policy-binding N times on 400/does-not-exist), not just poll `describe` before it |
