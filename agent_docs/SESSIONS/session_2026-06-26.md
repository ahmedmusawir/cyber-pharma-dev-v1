# Session 2026-06-26 — Post-FFM housekeeping (ESLint + navigation spinner)

> 📒 **Up to date in 3 reads:** `RECOVERY.md` → **`phase2.md`** (repo root — master Phase 2 reference) → this file. That's the whole picture: "I'm up to date, Tony."
> Continues `session_2026-06-25.md` (Phase 2.2 C5→close). Branch: `linting-1`. All work below is **uncommitted on `linting-1`** (operator owns git).

---

## What happened today

### 1. ESLint setup + safe lint fixes (ported from DockBloxx)
- ESLint was never installed; `next lint` is gone in Next 16. Stood it up from zero: **flat config** (`eslint.config.mjs`, ESLint 9) spreading `eslint-config-next@16`'s native flat configs (FlatCompat crashed — config-next 16 is flat-native). DockBloxx rules byte-faithful (`no-unused-vars` + `no-explicit-any` = warn) + `^_` ignore for frozen Phase-7 params. Script: `"lint": "eslint ."`.
- Triaged the report with the operator (no blind auto-fix): fixed the 17 preset **errors** (require()→ESM imports + `types/tailwind-grid-auto-fit.d.ts` shim for the untyped grid-auto-fit plugin; empty-interface→type alias in input/textarea), **downgraded** `react-hooks/set-state-in-effect`→warn (review-later, incl. known Navbar fetchUser). Cleaned safe unused-vars — **Logout.tsx now logs the real error** (was a silent swallow) — deleted dead dup `logout/route-1.ts`, ignored `agent_docs/**`.
- Added **`scripts/lint-check.sh`** (executable) — manual grouped lint summary (errors/warnings by rule). Result: **0 errors**; remaining warnings are the intentional review-later set.

### 2. Moose-style navigation spinner
- Diagnosed: moose spins because its pages `await` server data (loading.tsx Suspense); owedbook/admin are instant client islands so it never fired.
- Built a **root-layout overlay** (`NavigationSpinner` in `src/app/layout.tsx`) showing `SpinnerLarge` on every primary-nav request across owedbook/admin/profile/moose. Driven by `useLinkStatus` probes (`LinkPendingProbe`) on desktop/sidebar links + direct `setPending` on mobile-menu & logo clicks; shared `useNavSpinner` store; **min-display ~400ms** (mock data is instant); cleared on `usePathname` change.
- Iterated via operator eyes-on (each green): mobile-menu owedbook/profile weren't firing (probe unmounts with the closing panel → set pending in the click handler); profile needed the overlay at **root** (it's outside AuthedShell); logo showed a *tiny* spinner (`(public)/loading.tsx` used `Spinner`) → upgraded that to `SpinnerLarge` + wired the logo click. Operator verdict: "picture perfect."
- Tradeoff noted: root overlay is full-viewport (navbar/sidebar dimmed under a translucent backdrop for ~400ms) rather than content-area-only.

## Verification
Every step green: **tsc 0 / jest 117·25 / build 0**, `scripts/lint-check.sh` **0 errors**.

## Open / handoff
- **Git (operator):** two clean changesets uncommitted on `linting-1` — *linting* and *spinner* (file lists in `RECOVERY.md` → Files in flight). Plus docs: `RECOVERY.md`, `phase2.md`, this file.
- **Flagged (not Claudy's):** unstaged deletion of `agent_docs/APP_FACTORY/STARTER_KIT_HANDBOOK_v1.0.md` — operator to restore or keep.
- **Parked:** nothing functional. (The original DockBloxx-linting "parked" item is now done.)

## State at close
Phase 2 fully complete (2.1 + 2.2) + ESLint + navigation spinner. See **`phase2.md`** for the full Phase-2 reference and Phase-7 carry-forward flags.

---

## PENDING_APPROVAL — `/docs` refresh + README index (2026-06-26, later session)

**Goal:** `/docs` documents only the starter-kit foundation (and parts are now stale post-Phase-2); README is a TODO stub. Refresh foundation docs, add docs for what we built, make README the index. Scope confirmed via operator Q&A: **full 3-doc new set** + **rewrite MANUAL_TESTING as a current smoke guide**.

**Stale facts found in source (grounded, not assumed):**
- `(superadmin)` route group DELETED — current groups: `(admin)`, `(auth)`, `(members)`, `(public)` + ungrouped `owedbook`, `profile`, `moose-portal`.
- `superadmin-add-user` route GONE — `api/auth` = confirm/login/logout/signup only. AUTHORIZATION.md's "One-Two Punch" section is dead.
- `AppRole` enum moved `get-user-role.ts` → `src/utils/app-role.ts` (still retains SUPERADMIN value, but no portal uses it).
- MANUAL_TESTING.md = "Superadmin Portal Manual Testing Guide" → obsolete.
- `package.json` name still `qr-next13-supabase-v1` (FLAG only, no rename).

**Create:** `docs/PROJECT_OVERVIEW.md`, `docs/APP_ARCHITECTURE.md` (component→service→Zustand→mock swap point), `docs/ROUTES_AND_SURFACES.md`.
**Modify:** `README.md` (→ index), `docs/ARCHITECTURE.md`, `docs/AUTHORIZATION.md`, `docs/TESTING.md`, `docs/MANUAL_TESTING.md` (rewrite); AUTHENTICATION.md + DATABASE_SETUP.md only if verification finds drift.
**NOT touched:** `src/**`, `agent_docs/**` (index-linked only), `phase2.md`, `RECOVERY.md`, `docs/change_logs/**`, `.sql` files, `package.json`.

**Status: DONE** (operator approved "go").

**Delivered:** 3 new docs (`docs/PROJECT_OVERVIEW.md`, `docs/APP_ARCHITECTURE.md`, `docs/ROUTES_AND_SURFACES.md`); README rewritten as the `/docs` index + quick-start from real npm scripts; `docs/TESTING.md` rewritten to the real 25-suite/117-test reality (removed dead `superadmin-add-user` suite, documented node+jsdom split, flagged empty `test:integration` path); `docs/MANUAL_TESTING.md` rewritten from the dead superadmin guide → OwedBook + Admin-Portal smoke walk + Gate M. Surgical fixes: `ARCHITECTURE.md` (route-group list, app-layer pointer), `AUTHORIZATION.md` (enum→`app-role.ts`, layout list, retired the One-Two-Punch superadmin-add-user section → kept as doctrine), `DATABASE_SETUP.md` (2 dead Superadmin-Portal tails). `AUTHENTICATION.md` verified clean — untouched. Grounded every claim in source (jest run: 117/25 green; route gating read from layouts). No `src/` changes → suite still 117/25.

## PENDING_APPROVAL — remove vestigial `(members)/members-portal` (2026-06-26, later)

**Discovery (read-before-write):** NOT clean dead code. (1) Live link: member `portalHref` in `UserMenu.tsx`+`MobileNav.tsx` (used by `NavbarHome` on `/`) points to `/members-portal` (also links a non-existent `/register`). The live authed `Navbar.tsx` already uses `/owedbook` + `/profile`. (2) The **richer ProfileForm** (avatar/initials, "Member" badge, Full Name, "contact admin" copy) lives in the vestige; `/profile/ProfileForm.tsx` is older and hardcodes role "Admin". Test `member/ProfileForm.test.tsx` (11) is coupled to the rich form (asserts "Member", "JM" initials, email-prefix fallback) → blind delete breaks 11 tests.

**Plan:** DELETE `src/app/(members)/` (whole group) + `src/components/layout/Sidebar.tsx` (orphaned). MODIFY: salvage rich form into `src/app/profile/ProfileForm.tsx`; repoint test import → `@/app/profile/ProfileForm`; `UserMenu.tsx`+`MobileNav.tsx` member portalHref `/members-portal`→`/owedbook` (label→OwedBook); sync `ROUTES_AND_SURFACES.md`+`PROJECT_OVERVIEW.md` (drop vestige note). NOT touched: owedbook/admin-portal/Navbar/AuthedShell/moose/services/mocks. Known wart: salvaged form hardcodes "Member" badge (mirror of current "Admin" wart) — proper fix = role-aware badge via server prop (offered, out of base scope). `/register` dead link flagged not fixed. Verify: tsc + jest 117/25 + build.

**Status: DONE** (operator approved "go, with the role-aware badge").

**Delivered:** Deleted `src/app/(members)/` (whole group) + orphaned `src/components/layout/Sidebar.tsx` (`git rm`). Salvaged the richer profile form into `src/app/profile/ProfileForm.tsx` and made its identity badge **role-aware** (new required `role: AppRole` prop; Admin/Superadmin → destructive accent, Member → success accent — avatar + badge both tinted). `src/app/profile/page.tsx` now fetches the canonical role via `getUserRole(user.id)` (fallback MEMBER) and passes it down. Repointed member portal link `/members-portal`→`/owedbook` (label→"OwedBook") in `UserMenu.tsx` + `MobileNav.tsx`. Repointed `__tests__/member/ProfileForm.test.tsx` import → `@/app/profile/ProfileForm` + passes `role` + added an admin-badge test (K9). Docs synced: `ROUTES_AND_SURFACES.md` (dropped the `(members)` bullet + cleanup flag; added `/register` stale-link flag), `ARCHITECTURE.md` (dropped `(members)` from group list). Triad: **tsc 0 / jest 118·25 / build 0**; `/members-portal` absent from build route tree, `/profile` present. Grep-clean: no `members-portal`/`layout/Sidebar` refs remain in `src`.

**Still open (separate, flagged):** `UserMenu`/`MobileNav` link to a non-existent `/register` route (logged-out marketing nav); `package.json` name still `qr-next13-supabase-v1`. Neither actioned.

---

**Surfaced (RESOLVED 2026-06-26):** legacy `(members)/members-portal` was a starter-kit vestige that duplicates `/profile` byte-for-byte and uses the forbidden non-mobile `hidden md:block w-[25rem]` sidebar — documented as a removal candidate in ROUTES_AND_SURFACES. Also: `package.json` name is still `qr-next13-supabase-v1` (flagged, not renamed).
